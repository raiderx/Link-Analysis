Link Analysis
-------------
Java web-application that finds images in a link given by user.

User is provided with the following abilities:
- to see the list of links in application (+ filter);
- to add new link in application
  Then application provide the user with ability to choose the method of
  analyzing: simple or full
- to see images found during analyzing particular link;

